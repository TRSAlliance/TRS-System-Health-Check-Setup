{
  "name": "trs-system-health-check-setup",
  "version": "1.0.0",
  "description": "TRS Alliance System Health Check",
  "scripts": {
    "start": "node src/index.js",
    "test": "jest",
    "health": "node scripts/health-check.js",
    "health:watch": "npx nodemon scripts/health-check.js",
    "health:ci": "node scripts/health-check.js --json"
  },
  "dependencies": {
    "nodemon": "^3.0.3"
  }
}
"scripts": {
  "build": "echo 'Nothing to build — diagnostic tool only'",
  "lint": "eslint . --ext .js",
  "trs:deploy": "npm run health:ci && git add . && git commit -m '🩺 TRS Health Check Auto-Deploy' || echo 'No changes to deploy'"
},
"devDependencies": {
  "eslint": "^8.57.0",
  "jest": "^29.7.0"
}
